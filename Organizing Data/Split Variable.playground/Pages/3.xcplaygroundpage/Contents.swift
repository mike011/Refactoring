//: [Previous](@previous)

import Foundation

// 3. Change all references of the variable up to its second assignment and test.

func printInfo(height: Int, width: Int) {
    var temp = 0
    let perimeter = 2 * (height + width)
    print(perimeter)
    temp = height * width
    print(temp)
}

//: [Next](@next)
